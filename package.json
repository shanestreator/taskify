{
  "name": "tasklist",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently 'cd ./client && yarn dev' 'cd ./server && yarn dev'",
    "run:install": "concurrently 'cd ./client && yarn install' 'cd ./server && yarn install'"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  },
  "devDependencies": {}
}
